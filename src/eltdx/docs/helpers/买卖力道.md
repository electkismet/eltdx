---
hide:
  - navigation
---

[← 返回接口目录](../index.md){ .interface-detail-back }

# 买卖力道

获取单只证券按分钟整理的买卖力道副图序列。

| 项目 | 内容 |
| --- | --- |
| 调用 | `client.helpers.buy_sell_strength(code, include_raw=False)` |
| 返回 | `MinuteAuxSeries` |
| 数据来源 | `0x051b` 分时副图 |

## 示例

```python
from eltdx import TdxClient

with TdxClient(timeout=3) as client:
    series = client.helpers.buy_sell_strength("sz000001")
    for row in series.rows[:5]:
        print(row.time_label, row.values)
```

返回序列按分钟排列，适合绘图或盘中比较；它不是逐笔成交明细。

## 真实返回样本

??? return-sample "真实返回 JSON · MinuteAuxSeries（前两条）"
    <div class="return-sample-meta">
      <div><span>采样标的</span><code>sz000001</code></div>
      <div><span>副图类型</span><code>buy_sell_strength</code></div>
      <div><span>返回类型</span><code>MinuteAuxSeries</code></div>
    </div>
    <p class="return-sample-note">真实采样；买卖力道序列只展示前两个分钟点。</p>

    ```json
    {
      "exchange": "sz",
      "code": "000001",
      "selector_raw": 0,
      "kind": "buy_sell_strength",
      "points": [
        {"index": 0, "time_label": "09:31", "series_a": 14004, "series_b": 7793, "buy_commission": 14004, "sell_commission": 7793},
        {"index": 1, "time_label": "09:32", "series_a": 11042, "series_b": 3359, "buy_commission": 11042, "sell_commission": 3359}
      ]
    }
    ```
