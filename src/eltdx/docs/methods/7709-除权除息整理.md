---
hide:
  - navigation
---

[← 返回接口目录](../index.md){ .interface-detail-back }

# 7709-除权除息整理

## 作用

从 `0x000f` 股本变迁里筛出除权除息事件，并整理成分红、配股价、送转股、配股字段。

| 项目 | 内容 |
| --- | --- |
| 调用方法 | `client.helpers.xdxr(code)` |
| 数据来源 | [`0x000f`](../COMMANDS_7709.md#cmd-0x000f) |
| 返回模型 | `list[XdxrRecord]` |

这是 SDK 本地整理方法，数据来自 GBBQ。它只保留 `category_raw == 1` 的除权除息记录。

## 示例

```python
from eltdx import TdxClient

with TdxClient(timeout=3) as client:
    records = client.helpers.xdxr("sz000001")
    for item in records:
        print(item.date, item.fenhong, item.songzhuangu)
```

## 参数

| 参数 | 含义 |
| --- | --- |
| `code` | 证券代码 |
| `refresh` | 是否跳过股本变迁缓存 |

## 解析字段

| `XdxrRecord` 字段 | 含义 |
| --- | --- |
| `code` | 完整代码 |
| `date` / `time` | 日期 / 派生时间 |
| `category` / `category_name` | 类别编号 / 类别名称 |
| `fenhong` | 分红 |
| `peigujia` | 配股价 |
| `songzhuangu` | 送转股 |
| `peigu` | 配股 |

## 来源关系

| 字段 | 来源 |
| --- | --- |
| `fenhong` | `CapitalChangeRecord.c1_value` |
| `peigujia` | `CapitalChangeRecord.c2_value` |
| `songzhuangu` | `CapitalChangeRecord.c3_value` |
| `peigu` | `CapitalChangeRecord.c4_value` |

这些字段主要给本地复权因子和除权除息表使用。普通取复权 K 线可以直接用 `client.bars.get(..., adjust="qfq")`。

## 真实返回样本

??? return-sample "真实返回 JSON · list[XdxrRecord]（2 条节选）"
    <div class="return-sample-meta">
      <div><span>采样标的</span><code>sz000001</code></div>
      <div><span>采样日期</span><code>2026-08-17</code></div>
      <div><span>返回类型</span><code>list[XdxrRecord]</code></div>
    </div>
    <p class="return-sample-note">真实采样；从 GBBQ 事件中筛选并整理，完整列表未展开。</p>

    ```json
    [
      {
        "code": "sz000001",
        "date": "1990-03-01",
        "category": 1,
        "category_name": "除权除息",
        "fenhong": 0.0,
        "peigujia": 3.56,
        "songzhuangu": 0.0,
        "peigu": 1.0
      },
      {
        "code": "sz000001",
        "date": "1991-05-02",
        "category": 1,
        "category_name": "除权除息",
        "fenhong": 3.0,
        "peigujia": 0.0,
        "songzhuangu": 4.0,
        "peigu": 0.0
      }
    ]
    ```
