---
hide:
  - navigation
---

[← 返回接口目录](../index.md){ .interface-detail-back }

# 7709-指定日期历史分时

## 作用

按日期查询某天的历史分时价格和分钟成交量，适合补单日历史分时。

| 项目 | 内容 |
| --- | --- |
| 主要调用 | `client.minutes.history(code, date)` |
| | | 底层接口 | [`0x0fb4`](../COMMANDS_7709.md#cmd-0x0fb4) |
| 返回模型 | `MinuteSeries` |

这个接口按明确日期查询。另一个“近期历史分时”接口覆盖窗口更短，但会多返回今开和均价字段。

## 示例

```python
from eltdx import TdxClient

with TdxClient(timeout=3) as client:
    series = client.minutes.history("sz000001", "2026-05-20")
    series2 = client.minutes.history("sz000001", "2026-05-20")
```

## 真实返回样本

Python 实际返回 `MinuteSeries`。下面从 `to_jsonable(series)` 中选取开盘后两分钟和 15:00 三个分时点；完整结果为 240 个点。

??? return-sample "真实返回 JSON · MinuteSeries（3 个分时点节选）"
    <div class="return-sample-meta">
      <div><span>采样标的</span><code>sz000001</code></div>
      <div><span>交易日期</span><code>2026-08-14</code></div>
      <div><span>返回类型</span><code>MinuteSeries</code></div>
    </div>
    <p class="return-sample-note">真实采样；完整返回 240 个分时点，JSON 仅作结构展示。</p>

    ```json
    {
      "exchange": "sz",
      "market_id": 0,
      "code": "000001",
      "trading_date": "2026-08-14",
      "prev_close": 11.25,
      "points": [
        {
          "index": 0,
          "time_label": "09:31",
          "time": "2026-08-14T09:31:00+08:00",
          "price": 11.21,
          "price_milli": 11210,
          "volume": 19938
        },
        {
          "index": 1,
          "time_label": "09:32",
          "time": "2026-08-14T09:32:00+08:00",
          "price": 11.2,
          "price_milli": 11200,
          "volume": 8863
        },
        {
          "index": 239,
          "time_label": "15:00",
          "time": "2026-08-14T15:00:00+08:00",
          "price": 11.11,
          "price_milli": 11110,
          "volume": 11639
        }
      ]
    }
    ```

## 参数

| 参数 | 含义 |
| --- | --- |
| `code` | 证券代码 |
| `trading_date` / `date` | 交易日，支持 `YYYY-MM-DD`、`YYYYMMDD`、`date` |
| `include_raw` | 是否保留原始 payload |

## 解析字段

| `MinuteSeries` 字段 | 含义 |
| --- | --- |
| `exchange` / `market_id` / `code` / `full_code` | 市场和代码 |
| `trading_date` | 交易日 |
| `prev_close` | 昨收 |
| `points` | 分时点列表 |
| `count` | 分时点数量 |
| `volume_sum` | 分时成交量合计 |
| `raw_payload` | 原始 payload |

| `MinutePoint` 字段 | 含义 |
| --- | --- |
| `index` | 分时序号 |
| `time_label` / `time` | 时间文本 / 带日期时间 |
| `price` / `price_milli` | 当前价格 / 毫厘价 |
| `volume` | 该分钟成交量，单位手 |
| `price_delta_raw` / `aux_delta_raw` | 协议差分原始字段 |
| `price_raw` | 价格差分累计后的原始价格口径 |
| `record_hex` | 单条原始十六进制 |

## 使用建议

| 场景 | 建议 |
| --- | --- |
| 要指定很早的某一天 | 优先用这个接口 |
| 只查近期交易日且想要均价 / 今开 | 可以用 [7709-近期历史分时.md](7709-近期历史分时.md) |
| 不传日期查主站当日数据 | 用 [7709-当日分时.md](7709-当日分时.md) |
