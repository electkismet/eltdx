# v3.1.7

`v3.1.7` 修正深交所创业板代码段识别。

## 主要变化

- 创业板按深交所完整 `30` 号段识别，覆盖 `300xxx`、`301xxx`、`302xxx` 等代码。
- `sz302132` 等代码现在正确归类为 `a_share`，板块为 `szse_chinext`。
- 每日涨跌停价和短线涨停状态判断同步使用完整 `30` 号段的创业板 20% 规则。
- 更新代码表文档并增加 `302xxx` 回归测试。

## 升级

```bash
python -m pip install --upgrade "eltdx==3.1.7"
```
