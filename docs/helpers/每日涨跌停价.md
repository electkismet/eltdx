---
hide:
  - navigation
---

[← 返回接口目录](../index.md){ .interface-detail-back }

# 每日涨跌停价

按实时昨收、市场板块、ST 和新股规则计算标准化涨跌停价格。

| 项目 | 内容 |
| --- | --- |
| 调用 | `client.helpers.daily_price_limits(codes=None)` |
| 返回 | `DailyPriceLimitTable` |
| 数据来源 | `0x054c` 昨收 + 本地业务规则 |

## 返回字段

每行 `DailyPriceLimit` 包含 `full_code`、`trade_date`、`pre_close`、`limit_up_price`、`limit_down_price` 和 `limit_rule`。

## 示例

```python
from eltdx import TdxClient

with TdxClient(timeout=3) as client:
    table = client.helpers.daily_price_limits(["sz000001", "sh600000"])
    for row in table.rows:
        print(row.full_code, row.limit_up_price, row.limit_down_price)
```

这是业务计算结果，不是服务器独立的“每日涨跌停价”协议。除权除息、ST、新股和板块规则按当前模型处理。

## 真实返回样本

??? return-sample "真实返回 JSON · DailyPriceLimitTable（1 条节选）"
    <div class="return-sample-meta">
      <div><span>采样标的</span><code>sz000001</code></div>
      <div><span>返回类型</span><code>DailyPriceLimitTable</code></div>
    </div>
    <p class="return-sample-note">真实采样；涨跌停价由昨收和当前市场规则在本地计算。</p>

    ```json
    {
      "codes": ["sz000001"],
      "rows": [
        {
          "full_code": "sz000001",
          "trade_date": "2026-08-17",
          "name": "平安银行",
          "pre_close": 11.2,
          "limit_up_price": 12.32,
          "limit_down_price": 10.08,
          "limit_ratio_pct": 10.0,
          "limit_rule": "main_10pct",
          "limit_status": "normal"
        }
      ]
    }
    ```
