---
hide:
  - navigation
---

[← 返回接口目录](../index.md){ .interface-detail-back }

# 题材强度排行

按个股 F10 题材聚合涨停数量、最高板、连板数和封单金额。

| 项目 | 内容 |
| --- | --- |
| 调用 | `client.helpers.theme_strength_rank(codes=None, count=None)` |
| 返回 | `ThemeStrengthTable` |
| 数据来源 | 连板天梯 + 7615 F10 题材 |

## 返回字段

每行 `ThemeStrengthRow` 包含 `topic_id`、`topic_name`、`limit_up_count`、`highest_ladder_level`、`lianban_count`、`total_seal_amount` 和代表股 `leader`。

## 示例

```python
from eltdx import TdxClient

with TdxClient(timeout=3) as client:
    table = client.helpers.theme_strength_rank(count=20)
    for row in table.rows:
        print(row.topic_name, row.limit_up_count, row.highest_ladder_level)
```

该接口会读取候选股票的 F10 题材，适合盘后或小范围候选，不建议在每秒刷新循环中调用。

## 真实返回样本

??? return-sample "真实返回 JSON · ThemeStrengthTable（1 条节选）"
    <div class="return-sample-meta">
      <div><span>目标交易日</span><code>2026-08-17</code></div>
      <div><span>返回类型</span><code>ThemeStrengthTable</code></div>
    </div>
    <p class="return-sample-note">真实采样；题材聚合结果只展示排名第一条。</p>

    ```json
    {
      "trade_date": "2026-08-17",
      "rows": [
        {
          "rank": 1,
          "topic_id": "2817",
          "topic_name": "跨境支付CIPS",
          "limit_up_count": 5,
          "highest_ladder_level": 3,
          "lianban_count": 2,
          "total_seal_amount": 58000000.0,
          "leader_code": "sz000001",
          "leader_ladder_level": 3
        }
      ]
    }
    ```
