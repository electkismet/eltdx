---
hide:
  - navigation
---

[← 返回接口目录](../index.md){ .interface-detail-back }

# 股票信息汇总

## 作用

一次查询某个或多个股票的常用表头信息，把行情、代码表和财务基础信息整理到同一行。

| 项目 | 内容 |
| --- | --- |
| 调用方法 | `client.helpers.stock_profile_table(codes)` |
| 返回模型 | `StockProfileTable` |
| 底层能力 | [`0x054c` 批量快照](../methods/7709-批量快照.md)、[`0x044d` 代码表](../methods/7709-代码表.md)、[`0x0010` 财务基础信息](../methods/7709-财务基础信息.md) |

## 示例

```python
from eltdx import TdxClient

with TdxClient(timeout=3) as client:
    table = client.helpers.stock_profile_table(["sz000001", "sh600000"])

for row in table.rows:
    print(row.full_code, row.name, row.last_price, row.change_pct, row.amount)
```

## 参数

| 参数 | 含义 |
| --- | --- |
| `codes` | 单个代码或代码列表 |
| `include_security` | 是否合并代码表里的名称、品种分类、板块分类，默认 `True` |
| `include_finance` | 是否合并财务基础信息里的股本、EPS、上市日期，默认 `True` |

## 返回字段

`StockProfileTable`

| 字段 | 含义 |
| --- | --- |
| `codes` | 请求代码 |
| `rows` | `StockProfile` 列表 |
| `count` | 行数 |

`StockProfile`

| 字段 | 含义 |
| --- | --- |
| `full_code` / `exchange` / `market_id` / `code` | 完整代码、市场和六位代码 |
| `name` | 证券名称 |
| `category` | 品种分类，例如 `a_share`、`etf`、`index` |
| `board` | 板块分类，例如主板、创业板、科创板、北交所 |
| `last_price` / `pre_close_price` | 最新价 / 昨收 |
| `open_price` / `high_price` / `low_price` | 今开 / 最高 / 最低 |
| `change` / `change_pct` | 涨跌额 / 涨跌幅 |
| `volume_hand` | 成交量，单位手 |
| `amount` | 成交额 |
| `open_amount_yuan` | 开盘金额 |
| `circulating_shares` / `total_shares` | 流通股本 / 总股本 |
| `turnover_rate` | 换手率，本地按成交量和流通股本计算 |
| `circulating_market_value` / `total_market_value` | 流通市值 / 总市值 |
| `eps` | 每股收益原始字段 |
| `ipo_date` / `updated_date` | 上市日期 / 财务数据更新日期 |
| `security` / `quote` / `finance` | 合并前的原始模型对象 |

## 返回示例

```python
StockProfile(
    full_code="sz000001",
    name="平安银行",
    category="a_share",
    board="main",
    last_price=12.0,
    pre_close_price=10.0,
    change_pct=20.0,
    volume_hand=5000,
    amount=6000000.0,
    circulating_shares=100000000.0,
    turnover_rate=0.5,
)
```

## 真实返回 JSON

??? return-sample "真实返回 JSON · StockProfileTable（1 条节选）"
    <div class="return-sample-meta">
      <div><span>采样标的</span><code>sz000001</code></div>
      <div><span>采样日期</span><code>2026-08-17</code></div>
      <div><span>返回类型</span><code>StockProfileTable</code></div>
    </div>
    <p class="return-sample-note">真实采样；security、quote、finance 三个嵌套原始模型未在节选中重复展开。</p>

    ```json
    {
      "codes": ["sz000001"],
      "rows": [
        {
          "full_code": "sz000001",
          "name": "平安银行",
          "category": "a_share",
          "board": "szse_main_board",
          "last_price": 11.1,
          "pre_close_price": 11.11,
          "open_price": 11.2,
          "high_price": 11.22,
          "low_price": 11.07,
          "change_pct": -0.0900090009000881,
          "volume_hand": 929043,
          "amount": 1031951488.0,
          "circulating_shares": 19405685000.0,
          "total_shares": 19405918750.0,
          "turnover_rate": 0.47874785146723753,
          "circulating_market_value": 215403103500.0,
          "total_market_value": 215405698125.0,
          "eps": 1.2400000095367432,
          "ipo_date": "1991-04-03",
          "updated_date": "2026-08-15"
        }
      ]
    }
    ```
