---
hide:
  - navigation
---

[← 返回接口目录](../index.md){ .interface-detail-back }

# 最新股票列表

获取当前代码表中的沪深北 A 股证券列表。

| 项目 | 内容 |
| --- | --- |
| 调用 | `client.helpers.latest_stock_list(market=None)` |
| 返回 | `list[SecurityCode]` |
| 数据来源 | `0x044d` 代码表 |

## 示例

```python
from eltdx import TdxClient

with TdxClient(timeout=3) as client:
    rows = client.helpers.latest_stock_list()
    print(rows[0].full_code, rows[0].name)
```

`market` 可选 `sh`、`sz` 或 `bj`；不传时返回全部沪深北 A 股。返回的是当前代码表快照，不包含历史上市或退市记录。

??? return-sample "真实返回 JSON · SecurityCode（2 条节选）"
    <div class="return-sample-meta">
    <div><span>返回类型</span><code>list[SecurityCode]</code></div>
    <div><span>采样接口</span><code>client.helpers.latest_stock_list()</code></div>
    </div>

    ```json
    [
      {"market": "sz", "code": "000001", "name": "平安银行"},
      {"market": "sz", "code": "000002", "name": "万科A"}
    ]
    ```

    <p class="return-sample-note">真实采样；完整结果包含当前代码表中的全部沪深北 A 股证券。</p>
