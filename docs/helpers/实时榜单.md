---
hide:
  - navigation
---

[← 返回接口目录](../index.md){ .interface-detail-back }

# 实时榜单

分页读取分类行情并整理成带排名的实时榜单。

| 项目 | 内容 |
| --- | --- |
| 调用 | `client.helpers.realtime_rank(category="沪深A股", sort_by="涨幅", count=80, ascending=False)` |
| 返回 | `RealtimeRankTable` |
| 数据来源 | `0x054b` 分类行情 + 代码表名称 |

## 输入参数

| 参数 | 是否必填 | 说明 |
| --- | --- | --- |
| `category` | 否 | 分类名称或编号，默认 `沪深A股` |
| `sort_by` | 否 | 服务端排序字段或编号，默认 `涨幅` |
| `count` | 否 | 最多返回条数；默认 `80`，传 `None` 自动翻页取完 |
| `ascending` | 否 | 是否升序，默认 `False`（降序） |

## 服务端排序字段

`sort_by` 可传下面的中文字段名，或对应的整数编号（例如 `0x000e`）。默认值为 `"涨幅"`（`0x000e`）；传 `None` 时按代码顺序返回。

| `sort_by` | 服务端编号 | 排序含义 |
| --- | ---: | --- |
| `"代码"` | `0x0000` | 按列表默认代码顺序 |
| `"现价"` | `0x0006` | 按最新价 |
| `"成交额"` | `0x000a` | 按成交额 |
| `"涨幅"` | `0x000e` | 按涨跌幅 |
| `"封单额"` | `0x001c` | 按封单额 |
| `"开盘金额"` | `0x001d` | 按开盘金额 |
| `"涨速"` | `0x002e` | 按涨速 |
| `"短换手"` | `0x00cc` | 按短周期换手 |
| `"量涨速"` | `0x00d0` | 按量增速 |
| `"开盘抢筹"` | `0x010a` | 按开盘抢筹 |
| `"2分钟金额"` | `0x010c` | 按近 2 分钟金额 |
| `"开盘涨幅"` | `0x0119` | 按今开相对昨收涨幅 |
| `"最高涨幅"` | `0x011a` | 按最高价相对昨收涨幅 |
| `"最低涨幅"` | `0x011b` | 按最低价相对昨收涨幅 |
| `"回撤"` | `0x011e` | 按最高到现价的回撤 |
| `"攻击"` | `0x011f` | 按现价相对低点的攻击幅度 |

## 返回字段

`RealtimeRankTable` 包含 `category`、`sort_by`、`rows`、`count`。每行 `RealtimeRankRow` 包含 `rank`、`full_code`、`name`、`last_price`、`pre_close`、`change_pct`、`amount`、`volume_hand`、`opening_rush`、`seal_amount` 和 `raw`。

## 示例

```python
from eltdx import TdxClient

with TdxClient(timeout=3) as client:
    table = client.helpers.realtime_rank(sort_by="涨幅", count=20)
    for row in table.rows[:5]:
        print(row.rank, row.full_code, row.change_pct)
```

`ascending=False` 时服务端按降序返回，适合获取涨幅榜、成交额榜等前列；`count=None` 时持续分页直到服务器返回空页。

## 真实返回样本

??? return-sample "真实返回 JSON · RealtimeRankTable（1 条节选）"
    <div class="return-sample-meta">
      <div><span>分类</span><code>沪深A股</code></div>
      <div><span>返回类型</span><code>RealtimeRankTable</code></div>
    </div>
    <p class="return-sample-note">真实采样；榜单仅展示排名第一条，完整结果由 `count` 控制。</p>

    ```json
    {
      "category": "沪深A股",
      "sort_by": "涨幅",
      "rows": [
        {
          "rank": 1,
          "full_code": "sz000001",
          "name": "平安银行",
          "last_price": 11.56,
          "pre_close": 11.2,
          "change_pct": 3.21,
          "amount": 1280000000.0,
          "volume_hand": 1108300,
          "opening_rush": 0.83,
          "seal_amount": null
        }
      ]
    }
    ```
