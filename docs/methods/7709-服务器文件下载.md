---
hide:
  - navigation
---

[← 返回接口目录](../index.md){ .interface-detail-back }

# 7709-服务器文件下载

## 作用

使用 `0x06b9` 分块读取接口，循环下载服务器上的完整文件。`download_file()` 会根据服务端实际返回长度推进偏移，直到收到空块或短块。

| 项目 | 内容 |
| --- | --- |
| 主要调用 | `client.resources.download_file(path, chunk_size=30000, max_bytes=None)` |
| 底层接口 | [`0x06b9`](../COMMANDS_7709.md#cmd-0x06b9) |
| 返回模型 | `bytes` |

## 示例

```python
from eltdx import TdxClient

with TdxClient(timeout=3) as client:
    payload = client.resources.download_file("zhb.zip")

print(len(payload))
```

`chunk_size` 控制每次读取的块大小，`max_bytes` 可限制总下载量。文件长度恰好是块大小整数倍时，下载器会再请求一次并在收到空块后结束。

## 参数

| 参数 | 含义 |
| --- | --- |
| `path` | 服务器文件路径，例如 `zhb.zip` |
| `chunk_size` | 每次读取的字节数，默认 `30000` |
| `max_bytes` | 最大总下载字节数；传 `None` 表示不限制，默认 `None` |

## 返回字段

返回文件完整内容的 `bytes`。该方法不返回分页元数据；需要查看每个块的 `offset`、`chunk_len` 和 `is_last` 时使用 [服务器文件读取](7709-服务器文件读取.md) 的 `FileContentChunk`。

| 返回值 | 含义 |
| --- | --- |
| `bytes` | 按服务端顺序拼接后的完整文件内容 |

如果只需要读取一个文件块，请使用 [服务器文件分块读取](7709-服务器文件读取.md)。如果要解析 `zhb.zip` 中的统计文件，请使用 [服务器统计文件解析](7709-服务器统计文件解析.md)。

## 真实返回样本

??? return-sample "真实返回 JSON · download_file()（64 字节节选）"
    <div class="return-sample-meta">
      <div><span>服务器文件</span><code>zhb.zip</code></div>
      <div><span>下载限制</span><code>max_bytes=64</code></div>
      <div><span>返回类型</span><code>bytes</code></div>
    </div>
    <p class="return-sample-note">真实采样；方法实际返回 bytes，JSON 中以十六进制展示。</p>

    ```json
    {
      "path": "zhb.zip",
      "bytes": 64,
      "content_hex": "504b030414000000080032660f5d6be689ce60110000c1390000100000006164646564636f64655f626a2e636667859bcd721b451080ef54f11e4071d0feefbe"
    }
    ```
