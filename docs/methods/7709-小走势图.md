---
hide:
  - navigation
---

[← 返回接口目录](../index.md){ .interface-detail-back }

# 7709-小走势图

## 作用

查询单标的小型价格走势序列，适合列表页、概览页或轻量走势图。

| 项目 | 内容 |
| --- | --- |
| 调用方法 | `client.minutes.sparkline(code, selector=1, window=20)` |
| 底层接口 | [`0x0fd1`](../COMMANDS_7709.md#cmd-0x0fd1) |
| 返回模型 | `SparklineSeries` |

## 示例

```python
from eltdx import TdxClient

with TdxClient(timeout=3) as client:
    series = client.minutes.sparkline("sz000001", selector=1, window=20)
    print(series.base_price, series.prices)
```

## 参数

| 参数 | 含义 |
| --- | --- |
| `code` | 证券代码 |
| `selector` | 走势选择器，默认 `1` |
| `window` | 窗口 / 数量参数，默认 `20` |
| `include_raw` | 是否保留原始 payload，默认 `False` |

## 解析字段

| `SparklineSeries` 字段 | 含义 |
| --- | --- |
| `exchange` / `market_id` / `code` / `full_code` | 市场和代码 |
| `selector_raw` | 请求选择器 |
| `selector_echo` | 服务端回显的选择器 |
| `window_or_count_raw` | 请求窗口参数 |
| `max_count_raw` | 服务端返回最大数量口径 |
| `base_price` | 基准价 |
| `prices` | 价格序列 |
| `reserved_param_u32` | 保留参数原始值 |
| `count` | 实际价格点数量 |
| `raw_payload` | 原始 payload |

## 使用建议

这个方法返回的是轻量价格序列，字段比完整分时少。要画完整分时图，用 [7709-当日分时.md](7709-当日分时.md) 或 [7709-近期历史分时.md](7709-近期历史分时.md)。

## 真实返回样本

??? return-sample "真实返回 JSON · SparklineSeries（价格节选）"
    <div class="return-sample-meta">
      <div><span>采样标的</span><code>sz000001</code></div>
      <div><span>窗口参数</span><code>window=5</code></div>
      <div><span>返回类型</span><code>SparklineSeries</code></div>
    </div>
    <p class="return-sample-note">真实采样；prices 为轻量走势价格序列，下面只展示前两个点。</p>

    ```json
    {
      "exchange": "sz",
      "code": "000001",
      "selector_raw": 1,
      "selector_echo": 1,
      "window_or_count_raw": 5,
      "max_count_raw": 61,
      "base_price": 11.109999656677246,
      "prices": [11.199999809265137, 11.109999656677246],
      "reserved_param_u32": 2420
    }
    ```
