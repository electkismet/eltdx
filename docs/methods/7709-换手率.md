---
hide:
  - navigation
---

[← 返回接口目录](../index.md){ .interface-detail-back }

# 7709-换手率

## 作用

用成交量和指定日期股本本地计算换手率。服务器不直接返回这个结果。

| 项目 | 内容 |
| --- | --- |
| 调用方法 | `client.helpers.turnover(code, volume, on=None, unit="hand")` |
| 数据来源 | [`0x000f`](../COMMANDS_7709.md#cmd-0x000f) 的流通股本 |
| 返回 | `float` |

## 示例

```python
from eltdx import TdxClient

with TdxClient(timeout=3) as client:
    turnover = client.helpers.turnover("sz000001", 123456, on="2026-05-20", unit="hand")
    print(turnover)
```

## 参数

| 参数 | 含义 |
| --- | --- |
| `code` | 证券代码 |
| `volume` | 成交量 |
| `on` | 使用哪天之前最近一次流通股本 |
| `unit` | `hand` 表示手，`share` 表示股 |
| `refresh` | 是否跳过股本变迁缓存 |

`unit` 还支持 `hands`、`lot`、`lots`、`shares`、`stock` 这些英文别名。

## 计算

```text
换手率 = 成交股数 / 流通股本 * 100
```

`unit="hand"` 时，传入成交量会先乘以 100 转成股。

## 数据来源

| 项目 | 来源 |
| --- | --- |
| 成交量 | 调用者自己传入，可以来自 K 线、分时或行情快照 |
| 流通股本 | `client.helpers.equity(code, on=...)` |
| 股本底层接口 | [7709-股本变迁GBBQ.md](7709-股本变迁GBBQ.md) |

如果指定日期之前找不到流通股本，返回 `0.0`。

## 真实返回样本

??? return-sample "真实返回 JSON · 换手率计算结果"
    <div class="return-sample-meta">
      <div><span>采样标的</span><code>sz000001</code></div>
      <div><span>交易日期</span><code>2026-08-14</code></div>
      <div><span>返回类型</span><code>float</code></div>
    </div>
    <p class="return-sample-note">真实采样；成交量 832343.76 手，按该日可用流通股本计算。</p>

    ```json
    {
      "code": "sz000001",
      "volume_hand": 832343.76,
      "trading_date": "2026-08-14",
      "turnover_pct": 0.428917484747382
    }
    ```
